# Secrets for stackBrowserAgent (base64 encoded)
# Note: Replace these with actual secrets in production
apiVersion: v1
kind: Secret
metadata:
  name: stackbrowseragent-secrets
type: Opaque
data:
  # jwt-secret: base64 encoded value
  # Example: echo -n "your-secret-key" | base64
  jwt-secret: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24K
  
  # database credentials (if needed)
  # db-username: base64 encoded value
  # db-password: base64 encoded value
  
  # API keys (if needed)
  # api-key: base64 encoded value
